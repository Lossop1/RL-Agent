# P4.3 远程完整测试说明

## 当前状态

**文件部署**: 已完成 (10个文件全部上传)
**完整测试**: 待执行 (服务器当前不可达)

## 已创建的测试脚本

### scripts/remote_install_and_test.py

功能:
1. 连接远程GPU服务器
2. 安装pytest和pydantic依赖 (使用--break-system-packages)
3. 验证核心模块导入
4. 运行43个单元测试
5. 运行7个集成测试
6. 生成完整验证报告

## 执行方法

当远程服务器恢复连接后，在本地执行:

```bash
python scripts/remote_install_and_test.py
```

或直接在远程服务器执行:

```bash
ssh -p 31376 root@183.147.142.40

# 进入工作目录
cd /root/gpufree-data/diag_runs/locomotion_console_20260724_2315_agent108000_complete

# 安装依赖
python3 -m pip install pytest pydantic --break-system-packages

# 运行单元测试
python3 -m pytest tests/autotuner/product/test_checkpoint_registry.py \
                  tests/autotuner/product/test_checkpoint_selector.py \
                  tests/autotuner/product/test_checkpoint_curator.py \
                  tests/autotuner/product/test_capability_promotion.py \
                  -v --tb=short

# 运行集成测试
python3 -m pytest tests/products/taili/blind_locomotion/test_checkpoint_integration.py -v --tb=short
```

## 预期结果

如果所有依赖正确安装，应该看到:
- 43个单元测试全部通过
- 7个集成测试全部通过
- 总计50个测试全部通过

## 连接问题排查

如果继续无法连接:
1. 检查服务器是否在线
2. 检查防火墙规则
3. 确认端口31376是否开放
4. 联系服务器管理员确认访问权限

## P4.3完成标准

- [x] P4.2文件部署到远程服务器
- [x] GPU环境可用性确认
- [x] 部署脚本创建
- [ ] 依赖安装 (待服务器恢复)
- [ ] 完整测试执行 (待服务器恢复)
- [ ] 50个测试全部通过验证

当前进度: 60% (部署完成，测试待执行)
